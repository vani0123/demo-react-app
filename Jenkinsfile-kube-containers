pipeline {
  agent {
    node {
      label 'nodejs'
    }

  }
  stages {
    stage('build and tag image') {
      steps {
        container('nodejs') {
          sh 'which docker'
        }
      }
    }
  }
}
