node(nodejs) {
    stages {
        stage('Build and Test') {
            container('nodejs') {
                stage('Build') {
                        sh 'npm install'
                        sh 'npm test'
                }
            }
        }
    }
    post {
        always {
            cleanWs()
        }
    }
}
