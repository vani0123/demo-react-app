pipeline {
  agent {
    node {
      label 'nodejs'
    }

  }
  stages {
    stage('build and test') {
      steps {
        container('nodejs') {
          sh 'which docker'
          sh 'npm install'
          sh 'npm test' 
        }
      }
    }
  }
}
