pipeline {
    agent any
    stages {
        stage('Build and Test') {
            steps {
                script {
                        sh 'npm install'
                        sh 'npm test'
                }
            }
        }
    }
    post {
        always {
            cleanWs()
        }
    }
}
